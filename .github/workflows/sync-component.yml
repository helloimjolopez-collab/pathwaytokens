# This workflow is DISABLED.
#
# SideNav-figmamake.html is now a hand-authored static HTML spec sheet
# (pure HTML/CSS, no JavaScript, all states visible simultaneously).
# It is NOT auto-generated from SideNav.html.
#
# When to manually update SideNav-figmamake.html:
#   - Design token values change (colours, radius, spacing)
#   - A new component state is added or renamed
#   - The CollapseButton behaviour changes
#   - A new breakpoint or shell variant is added
#
# The build-component.py script has also been updated to refuse to run,
# to prevent it accidentally overwriting the static file.
#
# on:               <- intentionally commented out so this workflow never triggers
#   push:
#     branches: [main]
#     paths:
#       - SideNav.html
